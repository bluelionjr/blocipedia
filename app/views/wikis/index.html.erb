<h1 class="text-center">Wikis</h1>

<div class="row">
  <div class="col-md-8">
    <% @wikis.each do |wiki| %>
      <div class="media">
        <div class="media-body">
          <h4 class="media-heading">
            <% if wiki.private == false %>
            <%= link_to " #{wiki.title}", wiki %>
            <% end %>

            <% if wiki.private && (current_user.admin? || current_user.premium?) %>
              <i class="glyphicon glyphicon-sunglasses"></i>
                  <%= link_to " #{wiki.title}", wiki %>
            <% end %>
          </h4>
        </div>
      </div>
    <% end %>
  </div>
  <% if policy(@wikis).create? %>
    <div class="col-md-4">
      <%= link_to "New Wiki", new_wiki_path, class: "btn btn-success" %>
    </div>
  <% end %>
</div>


<!-- <h1 class="text-center">Wiki Wiki</h1>

<div class="row">
	<div class="col-md-4 col-md-offset-4">
		<table class="table table-striped">
			<tbody>
				<% @wikis.each do |wiki| %>
					<tr class="media">
						<td>
							<% if wiki.private %>
								<i class="glyphicon glyphicon-sunglasses"></i>
							<% end %>
							<%= link_to " #{wiki.title}", wiki %>
						</td>
				 		<% if user_signed_in? && current_user == wiki.user %>
							<td><%= link_to "Edit", edit_wiki_path(wiki.id), class:"btn btn-success" %></td>
							<td><%= link_to "DELETE", wiki, method: :delete, class:"btn btn-danger", data: {confirm: "Are you sure?"} %></td>
						<% elsif user_signed_in? %>
							<td><%= link_to "Edit", edit_wiki_path(wiki.id), class:"btn btn-success" %></td>
						<% end %>
					</tr>
				<% end %>
			</tbody>
		</table>
		<div class="text-center">
			<% if user_signed_in? %>
				<%= link_to "Create a new Wiki", new_wiki_path, class: "btn btn-success" %>
			<% end %>
		</div>
	</div>
</div> -->
